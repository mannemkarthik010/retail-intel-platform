# Core
pandas>=2.0
numpy>=1.24
scipy>=1.10
scikit-learn>=1.3
matplotlib>=3.7
joblib>=1.3   # persists the trained point/quantile GBMs (reports/models/*.joblib)
              # for reuse by src/explain.py and src/scenario.py -- see scripts/run_pipeline.py

# Serving
flask>=3.0

# Agent (real LLM backend, optional -- only needed if ANTHROPIC_API_KEY or
# OPENAI_API_KEY is set; both backends are plain HTTP via `requests`, no
# vendor SDK)
requests>=2.31

# Dev / testing
# (pytest is the standard choice; this repo's test suite also runs fine with
#  the stdlib `unittest` -- see docs/ARCHITECTURE.md for why)
# pytest>=7.4

# Production WSGI server (NOT installable in the build sandbox this repo was
# assembled in -- pip installs beyond a pre-cached set were blocked there, so
# this was never actually exercised end-to-end here). The Flask dev server
# (`python app/server.py`) prints its own "do not use in production" warning
# on startup; swapping it for gunicorn is the standard fix and requires no
# code changes beyond the run command:
#   pip install gunicorn
#   gunicorn -w 4 -b 0.0.0.0:8000 app.server:app
# gunicorn>=21.2

# NOTE on substitutions made in this build environment:
#   - statsmodels / lightgbm / fastapi could not be installed here (outbound
#     package installs beyond the pre-cached set were blocked). This repo
#     uses sklearn.HistGradientBoostingRegressor in place of LightGBM (same
#     algorithm family: histogram-based gradient boosting) and Flask in
#     place of FastAPI (same REST semantics, no async). Swapping either
#     back in touches only src/forecasting.py (model class) or app/server.py
#     (routing syntax) -- nothing else depends on the specific library.
